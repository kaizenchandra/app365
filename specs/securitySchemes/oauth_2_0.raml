description: |
  app365 API uses the OAuth 2.0 protocol for simple, but effective
  authentication and authorization. The one thing to keep in mind is tha
  all requests to the API must be made over SSL (https:// not http://)
type: OAuth 2.0
describedBy:
  headers:
    Authorization:
      description: |
        Used to send a valid OAuth 2 access token. Do not use together with
        the "access_token" queryParameters string parameter.
  queryParameters:
    access_token:
      description: |
        Used to send a valid OAuth 2 access token. Do not use together with
        the "Authorization" header
settings:
  authorizationUri: https://auth.applepolitical.com/oauth/authorize
  accessTokenUri: https://auth.applepolitical.com/oauth/access_token
  authorizationGrants: [ client_credentials , password ]
